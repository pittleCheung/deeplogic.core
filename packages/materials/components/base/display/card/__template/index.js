// import { exportComponents } from '../../../../_utils/utils';

// const requireComponent = require.context('./', true, /\.jsx$/);

// export default exportComponents(requireComponent);


// import { handleComponents } from "../../../../../index"

// const requireComponent = import.meta.glob("./**/*.jsx", { eager: true })

// const components = handleComponents(requireComponent)

// export default handleComponents(components)


// 默认不处理
export default () => {}