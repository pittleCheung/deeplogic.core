import { css } from '@emotion/css';

export const segmented = (token) => css`
  .ant-segmented-item-label {
    display: flex;
    align-items: center;
    justify-content: center;
  }
`;

export const imageForm = (token) => css`
  .ant-form-item-label {
    padding: 0;
  }
`;
